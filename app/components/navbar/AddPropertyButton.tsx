import React from "react";

type Props = {};

function AddPropertyButton({}: Props) {
  return (
    <div className="p-2 font-semibold text-sm rounded-full hover:bg-gray-200 cursor-pointer">
      djangobnb your home
    </div>
  );
}

export default AddPropertyButton;
